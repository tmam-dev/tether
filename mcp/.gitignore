/node_modules
/dist

# Env files (NEVER commit secrets)
.env
.env.*
!.env.example
!.env.template
!.env.sample
src/.env
src/.env_*

# Certificates / private keys (NEVER commit)
*.pem
*.key
*.crt
*.p12
*.pfx
